# Build Environment

language: python

python:
  - 3.0

install: true

script:
- docker run --privileged=true --cap-add=SYS_ADMIN -v $(pwd)/29:/root/enjoy/ davidva/unibuild-29 /bin/sh -c "rm -f /urpms && wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms && chmod a+x urpms && /urpms -g UnitedRPMs/libtxc_dxtn -s libtxc_dxtn.spec -t unitedrpms-29-i386 -c true"    

notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj




