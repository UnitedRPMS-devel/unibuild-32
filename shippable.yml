# Build Environment

language: python

python:
  - 3.0

install: true

script:
- docker run --privileged=true --cap-add=SYS_ADMIN davidva/unibuild-29 /bin/sh -c "rm -f /urpms && wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms && chmod a+x urpms && /urpms -g UnitedRPMs/mixxx -s mixxx.spec -t unitedrpms-28-x86_64 -u true -c true"  

notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





