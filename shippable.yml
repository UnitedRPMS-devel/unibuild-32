# Build Environment

language: python

python:
  - 3.0

install: true

script:
- docker run --privileged=true --cap-add=SYS_ADMIN -v $(pwd):/root/enjoy/ davidva/unibuild-31 /bin/sh -c "rm -f urpms && wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms && chmod a+x urpms && /urpms -g UnitedRPMs/gstreamer1 -s gstreamer1.spec -t unitedrpms-30-i386 -c true -u true"   

notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





