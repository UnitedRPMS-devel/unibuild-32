# Build Environment

language: python

python:
  - 3.0

install: true

script:
- docker run -v $(pwd)/29:/root/enjoy/ --privileged --cap-add=ALL davidva/unibuild-30 /bin/sh -c "rm -f /urpms && wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms && chmod a+x urpms && /urpms -g UnitedRPMs/steam -s steam.spec -t unitedrpms-29-x86_64 -c true"  

notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





